services:
  - type: web
    name: emaus-barbearia-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
      - key: MERCADO_PAGO_ACCESS_TOKEN
        sync: false
